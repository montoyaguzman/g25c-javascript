let num1 = 10;
let num2 = 20;


if (num1 <= num2) {
    // < y <= Menor y menor igual
    // > y >= Mayor y mayor igual
    // == Igual (compara valor)
    // === Exactamente igual (compara tipo)
    console.log('dentro del if');
}